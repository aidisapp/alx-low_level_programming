#include <stdio.h>

/**
 * main - this is the Entry point
 *
 * Description: this function prints out the name of the file
 *
 * it was compiled from , followed by a new line
 *
 * Return: this is always zero at success
 */

int main(void)
{
	printf("%s\n", __FILE__);
	return (0);
}
