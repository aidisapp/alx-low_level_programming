#include <stdio.h>
#include "main.h"

/**
 * _abs - this is the main block or the Entry point
 *
 * Description: this computes the absolute value of an integer
 *
 * @n: the int to check
 *
 * Return: will always return the absolute value of int
 */

int _abs(int n)
{
	if (n >= 0)
	{
	return (n);
	}
	return (-n);
}
